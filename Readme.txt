1.Python Installed  
         To check run this command "python --version"

2. Install Required Packages
          Open a terminal in VS Code and run " pip install speechrecognition transformers torch" and "pip install pyaudio"

3. Steps to Run the Script in VS Code

       	Open VS Code
	Open Folder where notegen.py is located (File > Open Folder)
	Open the Script by clicking on notegen.py
	Run the Script in terminal as " python notegen.py"



